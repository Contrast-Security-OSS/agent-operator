apiVersion: agents.contrastsecurity.com/v1beta1
kind: AgentInjector
metadata:
  name: injection-dotnet-chaining
spec:
  enabled: true
  type: dotnet-core
  selector:
    labels:
      - name: app
        value: injection-dotnet-chaining
  connection:
    name: testing-agent-connection
  configuration:
    name: testing-agent-configuration
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: injection-dotnet-chaining
  labels:
    app: injection-dotnet-chaining
spec:
  replicas: 1
  selector:
    matchLabels:
      app: injection-dotnet-chaining
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: injection-dotnet-chaining
    spec:
      containers:
        - image: nginx
          name: nginx
          env:
            - name: LD_PRELOAD
              value: something
          resources:
            limits:
              cpu: '1'
              memory: 128M
